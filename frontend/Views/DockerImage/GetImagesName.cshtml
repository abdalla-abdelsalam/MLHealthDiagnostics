@model IEnumerable<WebApplication1.ViewModel.DockerImagesNameViewModel>

@{
    ViewBag.Title = "GetImagesName";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>GetImagesName</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.label_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label_name)
        </td>
        <td>
            @*@Html.ActionLink("Details", "Details", new { id=item.id }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.label_name })
        </td>
    </tr>
}

</table>
